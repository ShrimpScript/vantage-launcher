{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window and event permissions for the launcher shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
